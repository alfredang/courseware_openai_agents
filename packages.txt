# System dependencies for Streamlit Cloud (Debian/Ubuntu)
# Minimal dependencies for PDF and image processing

# Required for Pillow image processing
libjpeg-dev
zlib1g-dev

# Required for psycopg2 PostgreSQL driver
libpq-dev
